# Database
DATABASE_URL="postgresql://aggregator:aggregator_dev@localhost:5433/llm_aggregator?schema=public"

# Redis (optional — rate limiting disabled if unavailable)
REDIS_URL="redis://localhost:6380"

# Encryption — generate with: openssl rand -hex 32
KEY_ENCRYPTION_SECRET="change-me-64-char-hex-string"

# App
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Proxy for geo-restricted providers (optional)
# ANTHROPIC_PROXY="http://127.0.0.1:7897"
